---
description: Guided one-sitting setup — Product Brief, Design Brief (+ writing rules), Technical Brief, then AGENTS.md wire-up
---

Invoke the `setup` skill.

It owns the full procedure: the intro and brownfield check, the three briefs in
order (the design brief also writes `WRITING.md`), the wire-up of `AGENTS.md` +
`CLAUDE.md`, the between-step updates, and the resume instructions if the user
stops partway.
