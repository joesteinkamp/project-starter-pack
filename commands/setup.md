---
description: Guided one-sitting setup — Product Brief, Design Brief, Technical Brief, then orchestration
---

Invoke the `setup` skill.

It owns the full procedure: the intro and brownfield check, the three briefs in
order, orchestration into `AGENTS.md` + `WRITING.md` + the selected harness files,
the between-step updates, and the resume instructions if the user stops partway.
