---
description: Walk through the Design Brief (UX first, then UI) and write DESIGN.md
---

Invoke the `design-brief` skill.

It owns the full procedure: pre-flight, Phase A (UX foundation) before Phase B
(UI system), the guardrail and defaults passes, the optional `DESIGN.json` tokens
companion, preview, and the write to `DESIGN.md`.
