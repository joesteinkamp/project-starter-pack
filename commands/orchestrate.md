---
description: Synthesize PRODUCT.md, DESIGN.md, and CODE.md into AGENTS.md, WRITING.md, and the selected harness files
---

Invoke the `orchestrator` skill.

It owns the full procedure: inputs, pre-flight (including migration off a legacy
`AGENT.md`), harness selection, synthesis, preview, and the final summary.
`AGENTS.md` and `WRITING.md` are always written; harness files are opt-in.
