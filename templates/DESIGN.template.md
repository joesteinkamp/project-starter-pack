---
version: alpha
name: {{PROJECT_NAME}}
description: {{PROJECT_TAGLINE}}
colors:
  background: "{{COLOR_BACKGROUND}}"
  foreground: "{{COLOR_FOREGROUND}}"
  muted: "{{COLOR_MUTED}}"
  accent: "{{COLOR_ACCENT}}"
  border: "{{COLOR_BORDER}}"
  success: "{{COLOR_SUCCESS}}"
  warning: "{{COLOR_WARNING}}"
  danger: "{{COLOR_DANGER}}"
typography:
  display:
    fontFamily: "{{FONT_DISPLAY}}"
  body:
    fontFamily: "{{FONT_BODY}}"
    fontSize: "{{TYPE_BASE_SIZE}}"
    lineHeight: "{{TYPE_BODY_LINE_HEIGHT}}"
  mono:
    fontFamily: "{{FONT_MONO}}"
  scale:
    ratio: "{{TYPE_SCALE_RATIO}}"
spacing:
  unit: "{{SPACING_UNIT}}"
rounded:
  sm: "{{ROUNDED_SM}}"
  md: "{{ROUNDED_MD}}"
  lg: "{{ROUNDED_LG}}"
components:
  button:
    backgroundColor: "{colors.accent}"
    textColor: "{colors.background}"
    rounded: "{rounded.md}"
---

# Design

> Generated by `project-starter-pack`. Conforms to the [DESIGN.md](https://github.com/joesteinkamp/design.md) format: YAML token front matter + markdown rationale. Canonical section order: Overview → Colors → Typography → Layout → Elevation & Depth → Shapes → Components → Do's and Don'ts. UX foundation appears between Overview and Colors as a project-specific extension (UX context drives visual decisions).

## Overview

<!-- One-paragraph mood + 2-3 reference points (existing products, publications,
environments). E.g. "Editorial sanctuary; closer to Eye Magazine than to a
SaaS dashboard." -->

{{VISUAL_REGISTER}}

## UX Foundation

<!-- Project-specific section. Visual decisions answer to UX context. -->

### User knowledge

<!-- What we know about the people using this — mental model, vocabulary,
known pain points, the moment they decide to use this. -->

{{USER_KNOWLEDGE}}

### Information architecture

<!-- Top-level navigation, content model, key entities and relationships,
intended URL/route structure. -->

{{INFORMATION_ARCHITECTURE}}

### User flows

<!-- 2-4 primary flows (happy path), critical edge cases, empty / loading /
error / offline states.

Format:
- **Flow name**: trigger → step → step → outcome
- Edge cases: …
- Empty / error / loading: … -->

{{USER_FLOWS}}

### Interaction principles

{{INTERACTION_PRINCIPLES}}

### Accessibility & inclusion

{{ACCESSIBILITY_UX}}

### UX success metrics

{{UX_SUCCESS_METRICS}}

## Colors

<!-- Strategy: Restrained | Committed | Full Palette | Drenched.
Use OKLCH. Avoid pure black on body surfaces (pure white is fine). Specify
foreground, background, accent, and any system colors with their OKLCH
triplets. Tokens live in the YAML front matter; this section explains the
*why*. -->

**Strategy:** {{COLOR_STRATEGY}}

{{COLOR_TOKENS}}

## Typography

<!-- Display / body / mono pairing. Base size, scale ratio (≥1.25), line
height (≥1.4 for body), measure cap (65-75ch). Tokens live in the front
matter; this section explains pairing rationale. -->

{{TYPOGRAPHY}}

## Layout

<!-- Spacing scale, grid (or no grid), density stance, when cards are allowed
and when they aren't. -->

{{SPACING_LAYOUT}}

## Elevation & Depth

<!-- Shadow / elevation scale (or its absence). Stacking conventions. When
to use border vs shadow vs background-shift to indicate elevation. -->

{{ELEVATION}}

## Shapes

<!-- Corner radii, border treatments, iconography stroke. Reference
`rounded.*` tokens from the front matter. -->

{{SHAPES}}

## Components

<!-- The first 8-12 primitives that define the system: button, input, card,
surface, link, etc. Note unusual choices (sharp corners, no shadows at rest).
Component tokens belong in the YAML front matter under `components.*`. -->

{{COMPONENTS}}

### Motion

<!-- Default easing (recommend expo-out), default duration buckets,
prefers-reduced-motion stance, what we never animate. -->

{{MOTION}}

## Do's and Don'ts

<!-- Crisp ban list pulled from `guardrails/design-anti-patterns.md` and
`guardrails/ux-anti-patterns.md`, plus any project-specific bans. -->

### Don't

{{DESIGN_ANTI_PATTERNS}}

### UX bans

{{UX_ANTI_PATTERNS_INLINE}}
