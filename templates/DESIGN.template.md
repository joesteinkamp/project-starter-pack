# Design

> Generated by `project-starter-pack`. This brief covers **how the product is understood and used (UX)** before **how it looks (UI)**. Visual decisions answer to UX context, not the other way around.

---

## UX Foundation

### User knowledge

<!-- What do we know about the people using this? Insights from research,
interviews, support tickets, analytics. Personas with scenarios, not
demographics. The vocabulary they use (which the product must speak). Known
pain points the product is responsible for solving. -->

{{USER_KNOWLEDGE}}

### Information architecture

<!-- Top-level navigation. Content model and taxonomy. Key entities and how
they relate. Intended URL/route structure. -->

{{INFORMATION_ARCHITECTURE}}

### User flows

<!-- 2-4 primary flows (happy path), critical edge cases, empty / loading /
error / offline states, and the hand-offs between flows.

Format suggestion:
- **Flow name**: trigger -> step -> step -> outcome
- Edge cases: ...
- Empty / error / loading: ... -->

{{USER_FLOWS}}

### Interaction principles

<!-- Opinionated heuristics that bias every interaction in this product.
Defaults you can use as-is or replace:
- Recognition over recall
- Undo over confirm
- Progressive disclosure over upfront complexity
- Keyboard-first reachability for power flows
- System messages speak the user's language, not the system's -->

{{INTERACTION_PRINCIPLES}}

### Accessibility & Inclusion

<!-- Baseline: WCAG 2.1 AA. Specifics:
- Keyboard reachability for every interactive surface
- Visible, non-color focus states
- prefers-reduced-motion respected
- Reading level target (default: 8th grade)
- Internationalization stance -->

{{ACCESSIBILITY_UX}}

### UX Success metrics

<!-- How do we know the experience is working?
- Task success rate (top flows)
- Time to first value
- Activation / retention signals
- Qualitative (SUS, ease-of-use, support-ticket volume)
- What counts as a regression -->

{{UX_SUCCESS_METRICS}}

---

## UI System

### Visual register

<!-- Overall mood in one sentence + 2-3 reference points (existing products,
publications, environments). E.g. "Editorial sanctuary; closer to Eye Magazine
than to a SaaS dashboard." -->

{{VISUAL_REGISTER}}

### Color strategy

<!-- One of: Restrained | Committed | Full Palette | Drenched.
Use OKLCH. Avoid pure black on body surfaces (pure white is fine). Specify
foreground, background, accent, and any system colors with their OKLCH
triplets. -->

**Strategy:** {{COLOR_STRATEGY}}

{{COLOR_TOKENS}}

### Typography

<!-- Display / body / mono pairing. Base size, scale ratio (≥1.25), line
height (≥1.4 for body), measure cap (65-75ch). -->

{{TYPOGRAPHY}}

### Spacing & layout

<!-- Spacing scale, grid (or no grid), density stance, when cards are allowed
and when they aren't. -->

{{SPACING_LAYOUT}}

### Motion

<!-- Default easing (recommend expo-out), default duration buckets,
prefers-reduced-motion stance, what we never animate (e.g. layout
properties). -->

{{MOTION}}

### Component primitives

<!-- The first 8-12 components that define the system: button, input, card,
surface, link, etc. Note the unusual choices (sharp corners? no shadows at
rest?). -->

{{COMPONENTS}}

### UI Anti-patterns to avoid

<!-- The crisp ban list. Pulled from `guardrails/design-anti-patterns.md` and
extended with any project-specific bans. -->

{{DESIGN_ANTI_PATTERNS}}
